# Claude Code UI Environment Configuration
# Only includes variables that are actually used in the code

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
#API server
PORT=3001
#Frontend port
VITE_PORT=5173

# Uncomment the following line if you have a custom claude cli path other than the default "claude"
# CLAUDE_CLI_PATH=claude

# =============================================================================
# HTTPS CONFIGURATION
# =============================================================================

# Enable HTTPS mode (set to 'true' to enable)
USE_HTTPS=false

# Custom SSL certificate paths (optional)
# If not provided, self-signed certificates will be generated for development
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key

# Allowed hosts for Vite dev server (comma-separated)
# Example: VITE_ALLOWED_HOSTS=example.com,subdomain.example.com
# VITE_ALLOWED_HOSTS=
